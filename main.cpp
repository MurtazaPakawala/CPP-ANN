#include <iostream>


// making the NET class 
class Net {
    Net();

    void feedForward();
    void backProp();
    void getResult() const;

};


int main() {
    // there has to be neural network (providing some sort of structure)
    // which has layers
    // layers have neuron

    Net myNet = Net();
    myNet.fe
    return 0;
}